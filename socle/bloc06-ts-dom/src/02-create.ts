/* Quest 2 · Building a card
   createElement, textContent, setAttribute, append. No innerHTML. */
import type { Title } from "./types.ts";

export function createCard(title: Title): HTMLLIElement {
  // TODO
}

// Challenge ⭐: createCards(titles: Title[]): DocumentFragment
