/* Quest 1 · The champion
   Run `npm run play`: the lesson and the tests show up in the terminal.
   Forbidden here: Math.max, Math.min, sort. */
import type { Title } from "./types.ts";

export function maximum(numbers: number[]): number | undefined {
  // TODO
}

export function minimum(numbers: number[]): number | undefined {
  // TODO
}

export function bestTitle(titles: Title[]): Title | undefined {
  // TODO
}

// Challenge ⭐: positionOfMaximum(numbers: number[]): number
