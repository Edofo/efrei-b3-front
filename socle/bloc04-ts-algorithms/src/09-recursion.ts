/* Quest 9 · The tree of universes
   A leaf has `episodes`, an inner node has `children`. */
import type { CollectionNode } from "./types.ts";

export function totalEpisodes(node: CollectionNode): number {
  // TODO
}

export function flatten(node: CollectionNode): string[] {
  // TODO
}

export function depth(node: CollectionNode): number {
  // TODO
}

// Challenge ⭐: pathTo(node: CollectionNode, title: string): string[] | null
