/* Quest 4 · The counters */
import type { Title } from "./types.ts";

export function countGenres(titles: readonly Title[]): Record<string, number> {
  // TODO
}

export function mostFrequentGenre(titles: readonly Title[]): string | null {
  // TODO
}

export function ratingDistribution(titles: readonly Title[]): Record<string, number> {
  // TODO
}

// Challenge ⭐: top(counts: Record<string, number>, n: number): [string, number][]
